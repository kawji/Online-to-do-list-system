NEXT_PUBLIC_SUPABASE_URL=your-url
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-anon-key
# Optional alias if your project still uses the anon key name:
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Profile avatars: run supabase/migrations/001_avatars.sql in the Supabase SQL Editor
# (creates public.profiles + storage bucket "avatars" with RLS).
